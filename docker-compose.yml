services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web-pigmoney
    restart: always
    environment:
      - NODE_ENV=production
    ports:
      - "3002:3000" 
    networks:
      - webnet

networks:
  webnet: